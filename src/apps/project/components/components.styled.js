import { Card, Row } from 'antd';
import styled from 'styled-components';

export const ProjectFormCardStyled = styled(Card)`
  min-height: 200px;
`;

export const VerticalMarginRow = styled(Row)`
  margin: 10px 0;
`;
