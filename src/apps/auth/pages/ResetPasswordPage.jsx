import React from 'react';
import { useTitle } from "react-use";

const ResetPasswordConfirmPage = () => {
  useTitle('Смена пароля');

  return (
    <div>
      ResetPasswordConfirmPage
    </div>
  );
};

export default ResetPasswordConfirmPage;
